### Lab Architecture

The lab simulates an enterprise IAM monitoring setup where a Windows Server Domain Controller forwards security events to a Splunk Enterprise instance via the Splunk Universal Forwarder over a NAT-based VMware network.
